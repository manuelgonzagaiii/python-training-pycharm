import unittest

# TODO(author): replace with real checks.
# Test focus: Money is frozen (assignment raises), equal Moneys are equal and hash-equal, negative amount raises ValueError, currency normalized to upper-case, amount stored as Decimal.


class TestCase(unittest.TestCase):
    @unittest.skip("skeleton: this task has not been populated yet")
    def test_placeholder(self):
        self.fail("populate this task")
