# Checkpoint: argparse mental model

> **Phase:** User Interfaces: CLI, TUI & GUI  •  **Stage:** 40.6 of 7  •  **Type:** `choice`  •  **Status:** skeleton (to be populated)

## What you'll learn
- Confirm understanding of when argparse exits vs raises
- Distinguish type= conversion from choices= validation
- Recall how subcommand dispatch via set_defaults works

## Python features introduced
`add_subparsers`, `type= vs choices=`, `mutually_exclusive_group`, `store_true vs count`, `SystemExit on parse error`

## MiniERP increment
No code change — a knowledge check that consolidates the argparse model before moving to Typer.

---

<div class="hint" title="Author notes (remove when populated)">

**TODO(author):** replace this stub with the full task description, then put starter code in `task.py` and real checks in `tests/test_task.py`.

- **Starter idea:** Multiple-choice questions on argparse dispatch, mutual exclusion, and exit behaviour.
- **Test focus:** choice task — answers checked by EduTools, not unit tests.

</div>
