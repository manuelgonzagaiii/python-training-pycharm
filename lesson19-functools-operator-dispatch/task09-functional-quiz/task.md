# functools & operator review

> **Phase:** Iterators, Generators & Functional Tools  •  **Stage:** 19.9 of 11  •  **Type:** `choice`  •  **Status:** skeleton (to be populated)

## What you'll learn
- Predict cache hit/miss and recompute behavior
- Reason about which singledispatch handler is selected
- Recall the minimum methods total_ordering needs
- Choose partial vs lambda and reduce-with-initializer correctly

## Python features introduced
`lru_cache vs cache semantics`, `cached_property caching behavior`, `partial argument binding`, `singledispatch resolution rules`, `total_ordering requirements`, `reduce initializer importance`

## MiniERP increment
No code change: a closing knowledge check that consolidates the functools/operator toolkit before the phase's lazy reporting/export milestone is considered complete.

---

<div class="hint" title="Author notes (remove when populated)">

**TODO(author):** replace this stub with the full task description, then put starter code in `task.py` and real checks in `tests/test_task.py`.

- **Starter idea:** # Multiple-choice task — no code to write.
# Example question:
#   @total_ordering needs which methods to synthesize all comparisons?
#     A) __eq__ only   B) __lt__ only   C) __eq__ and one ordering op   D) all six

- **Test focus:** None (choice task: answer key embedded in task config, no unit tests).

</div>
