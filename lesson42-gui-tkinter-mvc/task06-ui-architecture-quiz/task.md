# Checkpoint: four front-ends, one core

> **Phase:** User Interfaces: CLI, TUI & GUI  •  **Stage:** 42.6 of 6  •  **Type:** `choice`  •  **Status:** skeleton (to be populated)

## What you'll learn
- Confirm the principle that all four UIs adapt the same service layer
- Choose the right geometry manager / TUI tool for a scenario
- Recall how event loops differ across argparse (none), curses, Textual, and tkinter

## Python features introduced
`MVC/presenter separation`, `geometry manager selection`, `tkinter mainloop vs textual/curses loops`, `stdlib-first then OSS rationale`, `service-layer reuse`

## MiniERP increment
No code change — capstone knowledge check that the CLI, TUI, and GUI all front the one MiniERP core, closing the phase.

---

<div class="hint" title="Author notes (remove when populated)">

**TODO(author):** replace this stub with the full task description, then put starter code in `task.py` and real checks in `tests/test_task.py`.

- **Starter idea:** Multiple-choice questions on MVC reuse, geometry managers, event loops, and the stdlib-first-then-OSS progression.
- **Test focus:** choice task — answers checked by EduTools, not unit tests.

</div>
